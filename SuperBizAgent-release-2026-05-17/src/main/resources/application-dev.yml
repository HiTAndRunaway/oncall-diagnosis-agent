# Development profile: security disabled
superbiz:
  security:
    enabled: false
    api-keys:
      - key: "dev-api-key-change-me"
        user-id: "admin"
        description: "Development-only API key (example)"

# SpringDoc OpenAPI - dev 环境启用 Swagger UI
springdoc:
  swagger-ui:
    enabled: true
