**SuperBizAgent — 企业级智能运维与知识管理平台**

基于 **Spring Boot 3.2 + Spring AI Alibaba Agent Framework** 构建，核心实现两大 AI 能力：

**一、RAG 智能检索链**
集成 **Milvus 向量数据库**与 DashScope `text-embedding-v4`，实现从文档解析（TXT/PDF/Markdown）→ 多策略分块（标题切分、定长、语义边界、Parent-Child 小大检索）→ 双路混合召回（Dense 向量 + BM25 稀疏向量，**RRF 融合**）→ **gte-rerank-v2 重排序**→ `qwen3-max` 生成答案的完整流水线。支持查询改写（假设性答案/HyDE、摘要抽象等多策略）、Redis 缓存、以及 **Agentic RAG** 模式（问题拆解→多轮迭代检索→相关性评估→Query 精炼闭环，带轮次上限与降级兜底）。

**二、AIOps 多智能体编排**
采用 **SupervisorAgent** 驱动 **Planner-Executor-Replanner** 循环：Planner 分解告警并决策（PLAN→EXECUTE→FINISH），Executor 执行工具调用并结构化反馈，Supervisor 评估进展直至输出含活跃告警表、根因分析、处理方案的完整 Markdown 报告。工具链覆盖 Prometheus 告警查询、腾讯云 CLS 日志检索（通过 **MCP 协议**动态注入）、内部知识库语义搜索。

会话管理基于 **Redis 三层存储**（摘要层 → 详情层 → 元数据层），配合滑动窗口（6 对消息）与 LLM 自动摘要生成。前端采用 **SSE 流式通信**，后端以 ReactAgent 流式输出实现打字机效果。全链路涵盖向量检索、多 Agent 协作、工具编排及流式交互，具备从知识管理到智能诊断的 AI 落地能力。
